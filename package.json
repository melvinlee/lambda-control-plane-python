{
  "name": "lambda-control-plane-python",
  "version": "1.0.0",
  "description": "FaaS control plane to start/stop EC2 and set autoscaling instance size. See `serverless.yml` example how to hook up event trigger for each function.",
  "main": "index.js",
  "dependencies": {
    "serverless-pseudo-parameters": "^1.4.2"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC"
}
